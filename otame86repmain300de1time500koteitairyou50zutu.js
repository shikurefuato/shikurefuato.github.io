const sap2ort = document.getElementById('saport');
let executionCount = 0;
let execution2Count = 0;
let execution3Count = 0;
let execution4Count = 0;
let execution5Count = 0;
let execution6Count = 0;
let execution7Count = 0;
let execution8Count = 0;
let execution9Count = 0;
let execution10Count = 0;
let execution11Count = 0;
let execution12Count = 0;
let execution13Count = 0;
let execution14Count = 0;
let execution15Count = 0;
let execution16Count = 0;
let execution17Count = 0;
let execution18Count = 0;
let execution19Count = 0;
let execution20Count = 0;
let execution21Count = 0;
let execution22Count = 0;
let execution23Count = 0;
let execution24Count = 0;
let execution25Count = 0;
let execution26Count = 0;

function switchDisplay() {
  executionCount++;

  const firstId = `show-first${executionCount}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display: block;" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2662431207" data-ad-format="auto" data-full-width-responsive="true"><!-- --></ins></p>`
  );
}

function switchDisplay2() {
  const currentId = `show-first${executionCount}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

function tinyDelay() {
  return Promise.resolve();
}


async function main() {

  switchDisplay();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switchDisplay2();

await tinyDelay();

}

async function repeatMain(times) {
  for (let i = 0; i < times; i++) {
    await main();
  }
}


function switch2Display() {
  execution2Count++;

  const firstId = `show-2first${execution2Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:728px;height:90px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3683800610"><!-- --></ins></p>`
  );
}

function switch2Display2() {
  const currentId = `show-2first${execution2Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma2in() {

  switch2Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch2Display2();

await tinyDelay();

}

async function repeat2Main(times) {
  for (let i = 0; i < times; i++) {
    await ma2in();
  }
}


function switch3Display() {
  execution3Count++;

  const firstId = `show-3first${execution3Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:336px;height:280px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3979485526"><!-- --></ins></p>`
  );
}

function switch3Display2() {
  const currentId = `show-3first${execution3Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma3in() {

  switch3Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch3Display2();

await tinyDelay();

}

async function repeat3Main(times) {
  for (let i = 0; i < times; i++) {
    await ma3in();
  }
}


function switch4Display() {
  execution4Count++;

  const firstId = `show-4first${execution4Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:250px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="1277027499"><!-- --></ins></p>`
  );
}

function switch4Display2() {
  const currentId = `show-4first${execution4Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma4in() {

  switch4Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch4Display2();

await tinyDelay();

}

async function repeat4Main(times) {
  for (let i = 0; i < times; i++) {
    await ma4in();
  }
}


function switch5Display() {
  execution5Count++;

  const firstId = `show-5first${execution5Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:50px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="6222505481"><!-- --></ins></p>`
  );
}

function switch5Display2() {
  const currentId = `show-5first${execution5Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma5in() {

  switch5Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch5Display2();

await tinyDelay();

}

async function repeat5Main(times) {
  for (let i = 0; i < times; i++) {
    await ma5in();
  }
}


function switch6Display() {
  execution6Count++;

  const firstId = `show-6first${execution6Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:160px;height:600px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="9893884112"><!-- --></ins></p>`
  );
}

function switch6Display2() {
  const currentId = `show-6first${execution6Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma6in() {

  switch6Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch6Display2();

await tinyDelay();

}

async function repeat6Main(times) {
  for (let i = 0; i < times; i++) {
    await ma6in();
  }
}


function switch7Display() {
  execution7Count++;

  const firstId = `show-7first${execution7Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:320px;height:50px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2435443333"><!-- --></ins></p>`
  );
}

function switch7Display2() {
  const currentId = `show-7first${execution7Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma7in() {

  switch7Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch7Display2();

await tinyDelay();

}

async function repeat7Main(times) {
  for (let i = 0; i < times; i++) {
    await ma7in();
  }
}


function switch8Display() {
  execution8Count++;

  const firstId = `show-8first${execution8Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:320px;height:100px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="6318616512"><!-- --></ins></p>`
  );
}

function switch8Display2() {
  const currentId = `show-8first${execution8Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma8in() {

  switch8Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch8Display2();

await tinyDelay();

}

async function repeat8Main(times) {
  for (let i = 0; i < times; i++) {
    await ma8in();
  }
}


function switch9Display() {
  execution9Count++;

  const firstId = `show-9first${execution9Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:250px;height:250px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3984125468"><!-- --></ins></p>`
  );
}

function switch9Display2() {
  const currentId = `show-9first${execution9Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma9in() {

  switch9Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch9Display2();

await tinyDelay();

}

async function repeat9Main(times) {
  for (let i = 0; i < times; i++) {
    await ma9in();
  }
}


function switch10Display() {
  execution10Count++;

  const firstId = `show-10first${execution10Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:200px;height:200px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="1374375624"><!-- --></ins></p>`
  );
}

function switch10Display2() {
  const currentId = `show-10first${execution10Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma10in() {

  switch10Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch10Display2();

await tinyDelay();

}

async function repeat10Main(times) {
  for (let i = 0; i < times; i++) {
    await ma10in();
  }
}


function switch11Display() {
  execution11Count++;

  const firstId = `show-11first${execution11Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:970px;height:90px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="6410144102"><!-- --></ins></p>`
  );
}

function switch11Display2() {
  const currentId = `show-11first${execution11Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma11in() {

  switch11Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch11Display2();

await tinyDelay();

}

async function repeat11Main(times) {
  for (let i = 0; i < times; i++) {
    await ma11in();
  }
}


function switch12Display() {
  execution12Count++;

  const firstId = `show-12first${execution12Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:468px;height:60px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2209417903"><!-- --></ins></p>`
  );
}

function switch12Display2() {
  const currentId = `show-12first${execution12Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma12in() {

  switch12Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch12Display2();

await tinyDelay();

}

async function repeat12Main(times) {
  for (let i = 0; i < times; i++) {
    await ma12in();
  }
}


function switch13Display() {
  execution13Count++;

  const firstId = `show-13first${execution13Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:600px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2413432033"><!-- --></ins></p>`
  );
}

function switch13Display2() {
  const currentId = `show-13first${execution13Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma13in() {

  switch13Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch13Display2();

await tinyDelay();

}

async function repeat13Main(times) {
  for (let i = 0; i < times; i++) {
    await ma13in();
  }
}


function switch14Display() {
  execution14Count++;

  const firstId = `show-14first${execution14Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:200px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3239428544"><!-- --></ins></p>`
  );
}

function switch14Display2() {
  const currentId = `show-14first${execution14Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma14in() {

  switch14Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch14Display2();

await tinyDelay();

}

async function repeat14Main(times) {
  for (let i = 0; i < times; i++) {
    await ma14in();
  }
}


function switch15Display() {
  execution15Count++;

  const firstId = `show-15first${execution15Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:100px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="4656451993"><!-- --></ins></p>`
  );
}

function switch15Display2() {
  const currentId = `show-15first${execution15Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma15in() {

  switch15Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch15Display2();

await tinyDelay();

}

async function repeat15Main(times) {
  for (let i = 0; i < times; i++) {
    await ma15in();
  }
}


function switch16Display() {
  execution16Count++;

  const firstId = `show-16first${execution16Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:970px;height:250px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="5040990605"><!-- --></ins></p>`
  );
}

function switch16Display2() {
  const currentId = `show-16first${execution16Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma16in() {

  switch16Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch16Display2();

await tinyDelay();

}

async function repeat16Main(times) {
  for (let i = 0; i < times; i++) {
    await ma16in();
  }
}


function switch17Display() {
  execution17Count++;

  const firstId = `show-17first${execution17Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:480px;height:320px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="8002195862"><!-- --></ins></p>`
  );
}

function switch17Display2() {
  const currentId = `show-17first${execution17Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma17in() {

  switch17Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch17Display2();

await tinyDelay();

}

async function repeat17Main(times) {
  for (let i = 0; i < times; i++) {
    await ma17in();
  }
}


function switch18Display() {
  execution18Count++;

  const firstId = `show-18first${execution18Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:320px;height:480px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2119313794"><!-- --></ins></p>`
  );
}

function switch18Display2() {
  const currentId = `show-18first${execution18Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma18in() {

  switch18Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch18Display2();

await tinyDelay();

}

async function repeat18Main(times) {
  for (let i = 0; i < times; i++) {
    await ma18in();
  }
}


function switch19Display() {
  execution19Count++;

  const firstId = `show-19first${execution19Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:234px;height:60px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3476729123"><!-- --></ins></p>`
  );
}

function switch19Display2() {
  const currentId = `show-19first${execution19Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma19in() {

  switch19Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch19Display2();

await tinyDelay();

}

async function repeat19Main(times) {
  for (let i = 0; i < times; i++) {
    await ma19in();
  }
}


function switch20Display() {
  execution20Count++;

  const firstId = `show-20first${execution20Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:180px;height:150px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="6959181701"><!-- --></ins></p>`
  );
}

function switch20Display2() {
  const currentId = `show-20first${execution20Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma20in() {

  switch20Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch20Display2();

await tinyDelay();

}

async function repeat20Main(times) {
  for (let i = 0; i < times; i++) {
    await ma20in();
  }
}


function switch21Display() {
  execution21Count++;

  const firstId = `show-21first${execution21Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:125px;height:125px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="7415872505"><!-- --></ins></p>`
  );
}

function switch21Display2() {
  const currentId = `show-21first${execution21Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma21in() {

  switch21Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch21Display2();

await tinyDelay();

}

async function repeat21Main(times) {
  for (let i = 0; i < times; i++) {
    await ma21in();
  }
}


function switch22Display() {
  execution22Count++;

  const firstId = `show-22first${execution22Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:120px;height:600px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="3476627495"><!-- --></ins></p>`
  );
}

function switch22Display2() {
  const currentId = `show-22first${execution22Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma22in() {

  switch22Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch22Display2();

await tinyDelay();

}

async function repeat22Main(times) {
  for (let i = 0; i < times; i++) {
    await ma22in();
  }
}


function switch23Display() {
  execution23Count++;

  const firstId = `show-23first${execution23Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:120px;height:240px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="6489803647"><!-- --></ins></p>`
  );
}

function switch23Display2() {
  const currentId = `show-23first${execution23Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma23in() {

  switch23Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch23Display2();

await tinyDelay();

}

async function repeat23Main(times) {
  for (let i = 0; i < times; i++) {
    await ma23in();
  }
}


function switch24Display() {
  execution24Count++;

  const firstId = `show-24first${execution24Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:120px;height:60px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="2397954318"><!-- --></ins></p>`
  );
}

function switch24Display2() {
  const currentId = `show-24first${execution24Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma24in() {

  switch24Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch24Display2();

await tinyDelay();

}

async function repeat24Main(times) {
  for (let i = 0; i < times; i++) {
    await ma24in();
  }
}


function switch25Display() {
  execution25Count++;

  const firstId = `show-25first${execution25Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:300px;height:1050px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="8347752160"><!-- --></ins></p>`
  );
}

function switch25Display2() {
  const currentId = `show-25first${execution25Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma25in() {

  switch25Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch25Display2();

await tinyDelay();

}

async function repeat25Main(times) {
  for (let i = 0; i < times; i++) {
    await ma25in();
  }
}


function switch26Display() {
  execution26Count++;

  const firstId = `show-26first${execution26Count}`;
  
  sap2ort.insertAdjacentHTML(
    'afterend',
    `<p id="${firstId}"><ins class="adsbygoogle" style="display:inline-block;width:1200px;height:238px" data-ad-client="ca-pub-5005673256143644" data-ad-slot="4430328190"><!-- --></ins></p>`
  );
}

function switch26Display2() {
  const currentId = `show-26first${execution26Count}`;
  const showFirst = document.getElementById(currentId);

  if (showFirst) {
    showFirst.classList.add("d-none");
  }
}

async function ma26in() {

  switch26Display();

  await tinyDelay();

(adsbygoogle = window.adsbygoogle || []).push({});

  await tinyDelay();

  switch26Display2();

await tinyDelay();

}

async function repeat26Main(times) {
  for (let i = 0; i < times; i++) {
    await ma26in();
  }
}


async function hyouji() {

  await repeat2Main(50);

  await tinyDelay();

  await repeat3Main(50);

  await tinyDelay();

  await repeat4Main(50);

  await tinyDelay();

  await repeat5Main(50);

  await tinyDelay();

  await repeat6Main(50);

  await tinyDelay();

  await repeat7Main(50);

  await tinyDelay();

  await repeat8Main(50);

  await tinyDelay();

  await repeat9Main(50);

  await tinyDelay();

  await repeat10Main(50);

  await tinyDelay();

  await repeat11Main(50);

  await tinyDelay();

  await repeat12Main(50);

  await tinyDelay();

  await repeat13Main(50);

  await tinyDelay();

  await repeat14Main(50);

  await tinyDelay();

  await repeat15Main(50);

  await tinyDelay();

  await repeat16Main(50);

  await tinyDelay();

  await repeat17Main(50);

  await tinyDelay();

  await repeat18Main(50);

  await tinyDelay();

  await repeat19Main(50);

  await tinyDelay();

  await repeat20Main(50);

  await tinyDelay();

  await repeat21Main(50);

  await tinyDelay();

  await repeat22Main(50);

  await tinyDelay();

  await repeat23Main(50);

  await tinyDelay();

  await repeat24Main(50);

  await tinyDelay();

  await repeat25Main(50);

  await tinyDelay();

  await repeat26Main(50);

  await tinyDelay();

  await repeatMain(300);

}


hyouji();


setTimeout(() => {
  repeatMain(300);
}, 500);

